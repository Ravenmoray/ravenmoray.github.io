// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// NOTE: replace with your actual GitHub Pages URL, e.g. https://karlmurray.github.io
// If you instead use a project repo (username.github.io/repo-name) rather than a
// user site (username.github.io), you'll also need to set `base: '/repo-name'`.
export default defineConfig({
  site: 'https://your-username.github.io',
});
